package pallindrom

import "practice_codes/data-structures/pallindrom_list/list"

func IsListPallindrom(head *list.LinkedList) (isPallindrom bool) {
	// split the list into 2 halves
	// reverse the second half
	//iterate over both the list and if anyone value is not matching then return false, else return true
	return

}
